import React from 'react';
import PropTypes from 'prop-types';
import * as S from './styles';

function Flux() {
  return (
    <S.StyledFlux>
      Flux
    </S.StyledFlux>
  );
}
export default Flux;

Flux.propTypes = {

};
